# Payment Application System

A simple C program demonstrating a basic payment application.

## Building

Run `make` to compile the program.

## Running

Execute `./payment_app` to run the application.